export default {
    // config options

    build:{
        rollupOptions: {
            output: {
                preserveModules: true
            }, 
            preserveEntrySignatures: 'strict',
        }
    }
  }